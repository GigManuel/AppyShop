{% extends 'AppydoQuizBundle::layout.html.twig' %}

{% block content %}
<!-- Top Content -->
        <div id="top-content">
          <span class="rubrique">{{entity.name}}</span>
          <div class="edit-quiz"><a href="{{path('manager_edit', {'id':entity.id})}}"></a></div>
        </div>
        <!-- Quiz Content -->
        <div id="list-content">
          <!-- Desc quiz -->
          
          <div class="desc-zoom-quiz">
  			<p>
            <span class="bold">Description :</span> {{entity.description}}
            </p>
{#    
            <div class="info-theme-quiz">
              <div class="theme-quiz">
              </div>
              <div class="title-theme-quiz">
                  <p>Thème bulle</p>
                  <p>12 questions max</p>
                  <p><div class="edit-quiz"><a href="#"></a></div></p>
              </div>
            </div>
          </div>
#}
          {% for subject in subjects %}
          <div class="item-zoom">
            <div class="meta-quiz">
              <span class="title-question"><div class="numero-question">Question {{ loop.index }}</div> <a href="{{path('subject_show', {'id':subject.id})}}">{{subject.name}}</a></span>
            </div>
            
            <div class="tools-quiz">
              <div class="suppr-quiz"><a href="{{path('subject_delete', {'id':subject.id})}}"></a></div>
              <div class="edit-quiz"><a href="{{path('subject_edit', {'id':subject.id})}}"></a></div>
            </div>
          </div>
          {% endfor %}
          <a href="{{ path('subject_new',{'manager':entity.id}) }}" class="add-quiz">ajouter une question</a>
        </div>
      </div>
{% endblock %}